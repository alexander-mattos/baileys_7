# Contributing

Thanks for your interest in contributing. Guidelines:

- Code style: follow existing TypeScript style.
- Tests: open a PR with small focused changes.
- Run `npm ci` and `npm run build` before raising PR.
- Keep `lib/` consistent with `src/`.

Create issues for bugs and feature requests.
